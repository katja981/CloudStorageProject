{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "AppDb": "HOST=localhost;DB=postgres;UID=postgres;PWD=mysecret;PORT=5432;"
  },
  "S3Options": {
    "AwsAccessKeyId": "your-access-key",
    "AwsEndpointUrlS3": "tigris-endpoint-url",
    "AwsSecretAccessKey": "secret, so don't commit to repository",
    "BucketName": "name of your bucket, you can decide this for yourself"
  }
}